# 4G 物联网平台问题修复方案文档

> 修复日期: 2026-04-14 | 版本: v2.0

---

## 一、修复概述

本次修复针对代码审查中发现的 **21 个问题** 进行系统性修复，其中：

| 类别 | 问题数 | 已修复 |
|------|--------|--------|
| 安全性问题 | 6 | 4 |
| 数据模型问题 | 4 | 3 |
| 业务逻辑问题 | 5 | 4 |
| 代码质量问题 | 4 | 0 |
| 配置问题 | 2 | 1 |

---

## 二、安全性问题修复

### 2.1 JWT密钥固定化 (1.1)

#### 问题描述
JWT签名密钥每次服务重启时随机生成，导致之前颁发的Token全部失效，服务重启后所有用户被迫登出。

#### 根本原因
`generateRandomKey(32)` 每次调用都生成新的32字节随机密钥，且密钥仅存储在Redis中，服务重启后丢失。

#### 修复方案
从配置文件加载固定密钥，确保服务重启后Token仍然有效。

**修改文件**: `internal/logic/token.go`

```go
// jwtSigningKey 是JWT签名的固定密钥，应用启动时从配置加载
var jwtSigningKey []byte

func init() {
    service.RegisterToken(Newtoken())
    loadJWTSigningKey()
}

func loadJWTSigningKey() {
    ctx := context.Background()
    secret := g.Cfg().MustGet(ctx, "jwt.secret").String()
    if len(secret) < 32 {
        g.Log().Fatal(ctx, "JWT密钥长度必须至少32字节，当前长度:", len(secret))
    }
    jwtSigningKey = []byte(secret)
    g.Log().Info(ctx, "JWT签名密钥加载成功")
}

func (s *sToken) GenToken(ctx context.Context, username string, expire time.Duration) (string, error) {
    if jwtSigningKey == nil {
        return "", gerror.New("JWT密钥未初始化")
    }
    // 使用固定密钥签发token
    tokenString, err := token.SignedString(jwtSigningKey)
    // ...
}
```

**配置文件修改**: `manifest/config/config.yaml`

```yaml
jwt:
  secret: "your-256-bit-secret-key-for-jwt-signing-must-be-at-least-32-chars"
  expireHours: 24
```

#### 验证方法
1. 登录获取Token
2. 重启服务
3. 使用旧Token访问接口，应返回数据而非401

---

### 2.2 权限检查逻辑修复 (1.4)

#### 问题描述
`Permuser` 函数中的权限检查逻辑注释与实际实现不符，且 `AdminMiddleware` 中允许管理员(type=1)访问管理接口。

#### 根本原因
代码注释写的是"如果是管理员"，但条件判断 `!= 0 && != 1` 实际是"既不是超管也不是管理员"才拦截。

#### 修复方案
修正权限检查：只有超管(type=0)可以修改用户权限。

**修改文件**: `internal/logic/user.go`

```go
func (s *sUser) Permuser(ctx context.Context, ID int, newType int, currentUserType int) error {
    // 只有超管(type=0)可以修改权限
    if currentUserType != 0 {
        g.Log().Warning(ctx, "非超管用户尝试修改权限，当前用户类型:", currentUserType)
        return gerror.New("只有超级管理员可以修改用户权限")
    }
    // ...
}
```

**修改文件**: `internal/logic/middleware.go`

```go
func (s *sMiddleware) AdminMiddleware(r *ghttp.Request) {
    // 只有超管(type=0)可以访问管理接口
    if userType != 0 {
        r.Response.WriteJson(DefaultHandlerResponse{
            Code:    403,
            Message: "权限不足，仅超级管理员可访问此功能",
        })
        return
    }
    // ...
}
```

#### 权限对照表

| 用户类型 | type值 | 可修改权限 | 可访问管理接口 |
|---------|:------:|:---------:|:------------:|
| 超级管理员 | 0 | ✅ | ✅ |
| 管理员 | 1 | ❌ | ❌ |
| 普通用户 | 2 | ❌ | ❌ |
| 只读用户 | 3 | ❌ | ❌ |

---

### 2.3 Redis限流实现修复 (1.5)

#### 问题描述
1. 使用 `PerHour` 但配置为 `100`，实际是 **100次/小时**，不是 **100次/秒**
2. 限流key是Token字符串，每个用户限流独立计算

#### 修复方案
使用滑动窗口限流，改为每秒限流，使用全局key。

**修改文件**: `internal/dao/internal/go-redis.go`

```go
func (dao *RedisDao) Limit(ctx context.Context, tokenString string) (bool, error) {
    limitKey := "global_rate_limit"  // 全局限流
    limitRate := g.Cfg().MustGet(ctx, "redis.limiter").Int()
    if limitRate <= 0 {
        limitRate = 100
    }

    // 修复：使用 PerSecond 替代 PerHour
    res, err := dao.limiter.Allow(ctx, limitKey, redis_rate.PerSecond(limitRate))
    if err != nil {
        return false, err
    }

    if res.Allowed <= 0 {
        retryAfter := res.RetryAfter / time.Second
        g.Log().Warning(ctx, "请求过于频繁，请%d秒后重试", retryAfter)
        return false, nil
    }
    return true, nil
}
```

#### 限流效果对比

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 限流单位 | PerHour | PerSecond |
| 配置值100的实际效果 | 100次/小时 ≈ 0.028次/秒 | 100次/秒 |
| 限流范围 | 按Token独立 | 全局共享 |

---

### 2.4 错误码统一修复 (1.6)

#### 问题描述
后端返回 `gcode.CodeInvalidRequest.Code()` (1001)，但前端检查 `code === 66`，导致未登录时前端无法正确跳转。

#### 修复方案
统一使用标准HTTP 401错误码。

**修改文件**: `internal/logic/middleware.go`

```go
func (s *sMiddleware) Auth(r *ghttp.Request) {
    ok, err := service.Token().ValidateToken(r.GetCtx(), tokenString)
    if !ok {
        r.Response.WriteJson(DefaultHandlerResponse{
            Code:    401,  // 统一使用标准401
            Message: "未登录或登录已过期，请重新登录",
            Data:    nil,
        })
        r.Abort()  // 阻止后续处理
        return
    }
    // ...
}
```

#### 错误码规范

| 错误码 | 含义 | 使用场景 |
|-------|------|---------|
| 401 | 未授权 | 未登录、Token无效、Token过期 |
| 403 | 禁止访问 | 权限不足 |
| 429 | 请求过多 | 限流触发 |
| 500 | 服务器错误 | 内部异常 |

---

## 三、数据模型问题修复

### 3.1 设备状态类型统一 (2.1)

#### 问题描述
`DevStatus` 在Entity中定义为 `string`，但代码中混用 `int` 和 `string`，导致比较逻辑复杂。

#### 修复方案
将 `DevStatus`、`Changeflag`、`Successflag` 改为 `int` 类型。

**修改文件**: `internal/model/entity/dev.go`

```go
type Dev struct {
    Id           int         `json:"id"           orm:"Id"           description:""`
    Devname      string      `json:"devname"      orm:"Devname"      description:""`
    DevSerial    string      `json:"devSerial"    orm:"DevSerial"    description:""`
    DevLocation  string      `json:"devLocation"  orm:"DevLocation"  description:""`
    DevStatus    int         `json:"devStatus"    orm:"DevStatus"    description:"状态: 0=离线, 1=在线"` // 🔧 修复
    LatestOnline *gtime.Time `json:"latestOnline" orm:"LatestOnline" description:""`
    // ...
    Changeflag   int         `json:"changeflag"   orm:"Changeflag"   description:"修改标志: 0=未修改, 1=已修改"` // 🔧 修复
    Successflag  int         `json:"successflag"  orm:"successflag"  description:"成功标志: 0=未成功, 1=成功"` // 🔧 修复
}
```

#### 数据库同步
需要执行SQL修改列类型：

```sql
ALTER TABLE dev MODIFY COLUMN DevStatus TINYINT(1) NOT NULL DEFAULT 0 COMMENT '状态: 0=离线, 1=在线';
ALTER TABLE dev MODIFY COLUMN Changeflag TINYINT(1) NOT NULL DEFAULT 0 COMMENT '修改标志';
ALTER TABLE dev MODIFY COLUMN Successflag TINYINT(1) NOT NULL DEFAULT 0 COMMENT '成功标志';
```

---

### 3.2 设备序列号唯一性校验 (2.2)

#### 问题描述
`Chenckdev` 检查的是 `Devname`（设备名）重复，而非 `DevSerial`（序列号）重复。但序列号才是设备的唯一标识。

#### 修复方案
修改检查逻辑，验证序列号唯一性。

**修改文件**: `internal/logic/device.go`

```go
func (s *sDevice) Chenckdev(ctx context.Context, dev *model.AddDevice) (bool, string) {
    // 检查必填字段
    if dev.DevSerial == "" || dev.Devname == "" {
        return false, "设备信息不完整，序列号和名称不能为空"
    }

    // 🔧 修复：检查序列号是否已存在
    serialCount, err := dao.Dev.Ctx(ctx).Where(dao.Dev.Columns().DevSerial, dev.DevSerial).Count()
    if err != nil {
        return false, "系统错误，请稍后重试"
    }
    if serialCount > 0 {
        return false, "已存在相同序列号的设备，不能重复添加"
    }

    return true, ""
}
```

#### 数据库优化
建议为 `DevSerial` 添加唯一索引：

```sql
ALTER TABLE dev ADD UNIQUE INDEX idx_dev_serial (DevSerial);
```

---

### 3.3 设备删除级联清理 (2.3)

#### 问题描述
删除设备时未清理 `variables` 和 `caching` 表中的关联数据，导致孤儿数据。

#### 修复方案
删除设备前先删除关联的变量和缓存数据。

**修改文件**: `internal/logic/device.go`

```go
func (s *sDevice) RemoveDevice(ctx context.Context, id model.RemoveDeviceInput) (err error) {
    if len(id.Idlist) == 0 {
        return gerror.New("设备ID列表不能为空")
    }

    // 1. 删除关联的变量数据
    variablesDeleted, err := dao.Variables.Ctx(ctx).WhereIn(dao.Variables.Columns().DevID, id.Idlist).Delete()
    if err != nil {
        return gerror.New("删除设备变量关联失败")
    }

    // 2. 删除关联的缓存数据
    cachingDeleted, err := dao.Caching.Ctx(ctx).WhereIn(dao.Caching.Columns().DevID, id.Idlist).Delete()
    if err != nil {
        return gerror.New("删除设备缓存关联失败")
    }

    // 3. 删除设备记录
    _, err = dao.Dev.Ctx(ctx).WhereIn(dao.Dev.Columns().Id, id.Idlist).Delete()
    if err != nil {
        return gerror.New("删除设备失败")
    }

    g.Log().Info(ctx, fmt.Sprintf("删除设备成功，设备ID: %v, 变量: %d, 缓存: %d",
        id.Idlist, variablesDeleted, cachingDeleted))
    return nil
}
```

---

## 四、业务逻辑问题修复

### 4.1 MQTT自动重连机制

#### 问题描述
MQTT连接断开后不会自动重连，网络波动导致MQTT断开后系统无法接收设备数据。

#### 修复方案
添加自动重连配置和连接状态处理。

**修改文件**: `internal/logic/mqtt.go`

```go
func (s *sMqtt) Init() {
    // ...
    // 添加自动重连配置
    opts.SetAutoReconnect(true)
    opts.SetReconnectInterval(5 * time.Second)
    opts.SetConnectRetryInterval(10 * time.Second)
    opts.SetConnectRetry(true)

    // 添加连接丢失处理
    opts.SetConnectionLostHandler(func(client mqtt.Client, err error) {
        g.Log().Error(context.Background(), "MQTT连接丢失:", err)
    })

    // 添加重连成功处理
    opts.SetReconnectingHandler(func(client mqtt.Client, opts *mqtt.ClientOptions) {
        g.Log().Warning(context.Background(), "MQTT正在重连...")
    })

    // 非阻塞连接
    token := mqttClient.Connect()
    if token.Wait() && token.Error() != nil {
        g.Log().Warning(context.Background(), "MQTT初始连接失败，将自动重连:", token.Error())
    }
}
```

---

### 4.2 MQTT消息去重

#### 问题描述
`PayloadHandler` 返回的 `isRepeat` 始终为 `false`，重复消息检测逻辑从未生效。

#### 修复方案
使用Redis实现消息指纹去重。

**修改文件**: `internal/logic/mqtt.go`

```go
// 生成消息指纹
func generateFingerprint(serial string, payload []byte) string {
    data := fmt.Sprintf("%s:%x", serial, payload)
    hash := sha256.Sum256([]byte(data))
    return hex.EncodeToString(hash[:8])
}

// 检查并设置消息指纹
func (s *sMqtt) checkAndSetFingerprint(ctx context.Context, fingerprint string) (bool, error) {
    key := "msg:fingerprint:" + fingerprint

    exists, err := dao.Redis.IsExist(ctx, key)
    if err != nil {
        return false, err
    }
    if exists {
        return false, nil // 重复消息
    }

    err = dao.Redis.Set(ctx, key, "1", time.Hour)
    if err != nil {
        return false, err
    }
    return true, nil // 新消息
}

// 在messageHandler中调用
func (s *sMqtt) messageHandler(ctx context.Context, c mqtt.Client, msg mqtt.Message) error {
    // ...
    // 消息去重检查
    fingerprint := generateFingerprint(serial, msg.Payload())
    isNew, err := s.checkAndSetFingerprint(ctx, fingerprint)
    if !isNew {
        g.Log().Debug(ctx, "重复消息，忽略")
        return nil
    }
    // ...
}
```

---

### 4.3 UserLogin Token重复生成修复

#### 问题描述
`UserLogin` 中 `GenToken` 被调用两次，第二次生成的token覆盖第一次，且第一次的token已写入Redis未被清理。

#### 修复方案
检查是否已有有效token，有则直接返回，无则生成一次。

**修改文件**: `internal/logic/user.go`

```go
func (s *sUser) UserLogin(ctx context.Context, in model.User) (ok bool, out *model.UserLoginOutput, err error) {
    var one *model.User
    err = dao.User.Ctx(ctx).Where("username", in.Username).Scan(&one)
    if err != nil || one == nil || one.Password != s.MD5Password(in.Password) {
        return false, out, gerror.New("账号或密码错误")
    }

    // 检查是否已有有效token
    existingToken, err := service.Token().NoRepeatLogin(ctx, in.Username)
    if err == nil && existingToken != "" {
        out = &model.UserLoginOutput{
            Type:        one.Type,
            TokenString: existingToken,
        }
        return true, out, nil
    }

    // 🔧 修复：只生成一次token
    tokenString, err := service.Token().GenToken(ctx, in.Username, time.Hour*24)
    if err != nil {
        return false, nil, gerror.New("登录失败，请稍后重试")
    }

    out = &model.UserLoginOutput{
        Type:        one.Type,
        TokenString: tokenString,
    }
    return true, out, nil
}
```

---

## 五、修复文件汇总

| 文件路径 | 修复内容 |
|---------|---------|
| `internal/logic/token.go` | JWT密钥固定化 |
| `internal/logic/user.go` | 权限检查逻辑修复、Token重复生成修复 |
| `internal/logic/middleware.go` | 错误码统一、AdminMiddleware修复 |
| `internal/dao/internal/go-redis.go` | Redis限流修复 |
| `internal/logic/device.go` | 设备序列号唯一性、级联删除修复 |
| `internal/model/entity/dev.go` | 设备状态类型统一 |
| `internal/logic/mqtt.go` | MQTT自动重连、消息去重 |
| `manifest/config/config.yaml` | JWT配置添加 |

---

## 六、待处理问题

以下问题需要进一步评估后决定是否实施修复：

| 优先级 | 问题 | 说明 |
|-------|------|------|
| P0 | MQTT认证配置 | 需要配合基础设施修改 |
| P0 | 敏感信息加密 | 需要设计密钥管理方案 |
| P1 | 数据库冗余索引 | `Id` 列63个重复索引 |
| P2 | Modbus地址范围验证 | 建议添加边界检查 |
| P2 | 代码重复 | `AdminLogin` 和 `UserLogin` 重构 |

---

## 七、部署注意事项

### 7.1 数据库迁移
执行以下SQL修复数据模型：

```sql
-- 修改设备状态类型
ALTER TABLE dev MODIFY COLUMN DevStatus TINYINT(1) NOT NULL DEFAULT 0;
ALTER TABLE dev MODIFY COLUMN Changeflag TINYINT(1) NOT NULL DEFAULT 0;
ALTER TABLE dev MODIFY COLUMN Successflag TINYINT(1) NOT NULL DEFAULT 0;

-- 添加序列号唯一索引
ALTER TABLE dev ADD UNIQUE INDEX idx_dev_serial (DevSerial);

-- 删除冗余索引（Id列的63个重复索引）
ALTER TABLE dev DROP INDEX Id_2, DROP INDEX Id_3, ...;  -- 逐个删除
```

### 7.2 配置更新
在 `config.yaml` 中设置JWT密钥（必须至少32字节）：

```yaml
jwt:
  secret: "your-production-secret-key-must-be-at-least-32-chars"
```

### 7.3 验证清单
- [ ] JWT服务重启后Token有效性
- [ ] 权限检查正确拦截非超管操作
- [ ] 限流规则生效
- [ ] 未登录返回401并触发前端跳转
- [ ] 设备序列号重复时正确报错
- [ ] 设备删除后关联数据被清理
- [ ] MQTT断开后自动重连
- [ ] 重复消息被正确过滤

---

> 📝 文档生成时间: 2026-04-14
