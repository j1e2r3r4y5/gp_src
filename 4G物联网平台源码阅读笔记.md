# 4G 物联网平台源码阅读笔记

> 前后端分离的 4G DTU 设备管理与监控平台 | 综合精炼版

---

## 📋 目录

- [一、项目概述](#一项目概述)
- [二、技术栈详解](#二技术栈详解)
- [三、项目结构分析](#三项目结构分析)
- [四、快速开发指南](#四快速开发指南)
- [五、系统功能需求](#五系统功能需求)
- [六、通信协议详解](#六通信协议详解)
- [七、数据库设计](#七数据库设计)
- [八、API 接口文档](#八api接口文档)
- [九、核心模块分析](#九核心模块分析)
- [十、数据流转路径](#十数据流转路径)
- [十一、配置文件说明](#十一配置文件说明)
- [十二、用户操作流程](#十二用户操作流程)
- [十三、系统部署架构](#十三系统部署架构)
- [十四、常见问题与解决方案](#十四常见问题与解决方案)
- [十五、总结](#十五总结)

---

## 一、项目概述

本项目是一个**前后端分离**的 4G 物联网设备管理平台，用于管理和监控 **4G DTU（数据传输单元）** 设备。

### 1.1 什么是 4G DTU？

**DTU (Data Transfer Unit)** 是数据传输单元，是一种用于将串口数据通过 4G 无线网络进行传输的工业设备。

| 特点 | 说明 |
|------|------|
| 远程通信 | 通过 4G 网络实现无线连接 |
| Modbus 协议 | 支持连接多种工业传感器和设备 |
| 数据透传 | 将下位机数据上传至云端服务器 |
| 远程控制 | 支持远程配置和控制，无需现场操作 |

### 1.2 业务架构

```
┌─────────────────────────────────────────────────────────────────┐
│  用户浏览器 (Vue 3)  ──HTTP──►  Go 后端服务 (端口 8000)        │
│                                    │                            │
│                         ┌──────────┼──────────┐                │
│                         ▼          ▼          ▼                │
│                      MySQL      Redis       MQTT               │
│                    (设备/用户)  (Token)    (设备通信)           │
│                                     │                            │
│                                     ▼                            │
│                                 InfluxDB                         │
│                                (时序数据)                        │
│                                     ▲                            │
│                                     │                            │
└─────────────────────────────────────┼─────────────────────────────┘
                                      │
                          ◄──── 4G DTU 设备群 (传感器/PLC)
```

---

## 二、技术栈详解

### 2.1 前端技术栈

```bash
Vue 3 + Vite + Element Plus + Axios + Vue Router 4
```

| 技术 | 用途 | 特点 |
|------|------|------|
| Vue 3 | 前端框架 | Composition API |
| Vite 7 | 构建工具 | 快速热更新 |
| Element Plus | UI 组件库 | 主题定制 |
| Axios | HTTP 客户端 | 拦截器支持 |
| Vue Router 4 | 路由管理 | SPA 支持 |

### 2.2 后端技术栈

```bash
Go + GoFrame + MySQL + Redis + InfluxDB + MQTT
```

| 技术 | 用途 | 特点 |
|------|------|------|
| Go | 服务端语言 | 高并发、编译型 |
| GoFrame | Web 框架 | 模块化、MVC |
| MySQL | 关系型数据库 | 设备/用户存储 |
| Redis | 缓存/会话 | Token 管理、限流 |
| InfluxDB | 时序数据库 | 传感器数据存储 |
| MQTT | 消息队列 | 设备通信 |
| JWT | 身份认证 | 无状态认证 |

---

## 三、项目结构分析

### 3.1 前端目录结构

```
4G_dev_front/4G_dev/
├── src/
│   ├── main.js                   # 入口文件
│   ├── App.vue                   # 根组件
│   ├── api/index.js              # API 接口
│   ├── components/              # 组件
│   │   ├── admin/               # 用户管理
│   │   ├── data/                # 数据展示
│   │   ├── variables/           # 变量管理
│   │   └── device.vue           # 设备管理
│   ├── router/index.js          # 路由配置
│   ├── utils/request.js         # Axios 配置
│   └── view/                    # 页面
│       ├── login.vue            # 登录页
│       └── home.vue             # 首页
└── package.json
```

### 3.2 后端目录结构

```
dev_back_end/dev/
├── main.go                       # 程序入口
├── manifest/config/config.yaml   # 配置文件
└── internal/
    ├── controller/               # 控制器层
    ├── service/                  # 服务接口
    ├── logic/                    # 业务逻辑 ⭐
    │   ├── device.go            # 设备管理
    │   ├── user.go             # 用户管理
    │   ├── mqtt.go             # MQTT 通信
    │   └── payload.go           # 协议解析 ⭐
    └── dao/                     # 数据访问层
```

### 3.3 文件映射

| 前端组件 | 后端逻辑 | 功能 |
|----------|----------|------|
| `device.vue` | `logic/device.go` | 设备管理 |
| `Administrator.vue` | `logic/user.go` | 用户管理 |
| `variables.vue` | `logic/data.go` | 变量配置 |
| `data.vue` | `logic/payload.go` | 数据展示 |
| `login.vue` | `logic/user.go` | 用户登录 |

---

## 四、快速开发指南

### 4.1 环境准备

**后端：**
```bash
Go 1.18+ | MySQL 5.7+ | Redis 6.0+ | InfluxDB 2.0+ | MQTT Broker
```

**前端：**
```bash
Node.js 16+ | npm 或 pnpm
```

### 4.2 启动步骤

**后端启动：**
```bash
cd "dev_back_end/dev"
go mod tidy
go run main.go
```

**前端启动：**
```bash
cd "4G_dev_front/4G_dev"
npm install
npm run dev
```

---

## 五、系统功能需求

### 5.1 核心管理功能

| 功能模块 | 功能描述 | 源码位置 |
|---------|---------|----------|
| **设备注册** | 添加新设备，记录序列号、名称、位置 | `logic/device.go` |
| **设备信息管理** | 修改设备基本信息 | `logic/device.go` |
| **设备删除** | 删除离线/故障设备 | `logic/device.go` |
| **设备查询** | 查看所有设备列表 | `logic/device.go` |
| **配置管理** | 配置发送模式、波特率等 | `logic/payload.go` |
| **变量管理** | 配置 Modbus 变量 | `variables.vue` |
| **配置下发** | 远程下发配置参数 | `logic/payload.go` |
| **远程控制** | 远程置数、重启设备 | `logic/payload.go` |

### 5.2 监控功能

| 功能模块 | 功能描述 | 源码位置 |
|---------|---------|----------|
| **设备状态监测** | 实时显示在线/离线状态 | `main.go` 定时任务 |
| **心跳检测** | 判断设备存活状态 | `logic/payload.go` |
| **数据采集监控** | 监控传感器上报数据 | `logic/payload.go` |
| **历史数据存储** | InfluxDB 时序数据库 | `logic/payload.go` |

### 5.3 用户权限管理

| 用户类型 | 类型值 | 权限说明 |
|---------|:------:|----------|
| 超级管理员 | 0 | 完全控制 |
| 管理员 | 1 | 管理设备和变量 |
| 普通用户 | 2 | 查看设备和数据 |
| 只读用户 | 3 | 仅查看设备列表 |

---

## 六、通信协议详解

### 6.1 上报协议（设备 → 服务器）

| 功能码 | 名称 | 数据格式 | 处理逻辑 |
|:------:|------|----------|----------|
| `00` | 心跳包 | 1 字节功能码 | 更新设备在线状态 |
| `01` | 上发模组配置 | 功能码 + 发送模式 + 配置数据 + 波特率 | 更新设备通信参数 |
| `02` | 模组配置结果 | 功能码 + 成功状态 (0-5) | 记录配置下发结果 |
| `03` | 数据配置上发 | 功能码 + 数量 + 各配置项 | 记录变量配置信息 |
| `04` | 数据配置结果 | 功能码 + 成功状态 | 同步缓存表和变量表 |
| `05` | 数据上发 | 功能码 + 数量 + 各数据项 | 解析数据并写入 InfluxDB |

### 6.2 下发协议（服务器 → 设备）

| 功能码 | 名称 | 数据格式 | 用途 |
|:------:|------|----------|------|
| `01` | 模组配置查询 | 1 字节功能码 | 查询设备当前配置 |
| `02` | 下发模组配置 | 功能码 + 发送模式 + 配置数据 + 波特率 | 配置设备通信参数 |
| `03` | 数据配置查询 | 1 字节功能码 | 查询设备变量配置 |
| `04` | 下发数据配置 | 功能码 + 数量 + 变量配置 | 配置设备上报变量 |
| `06` | 远程置数 | 功能码 + 类型 + 地址 + 数量 + 数值 | 远程控制设备输出 |

### 6.3 波特率配置表

| 值 | 波特率 | | 值 | 波特率 |
|:--:|:------:|-|:--:|:------:|
| 0x00 | 300 | | 0x08 | 28800 |
| 0x01 | 600 | | 0x09 | 38400 |
| 0x02 | 1200 | | 0x0A | 57600 |
| 0x03 | 2400 | | 0x0B | 76800 |
| 0x04 | 4800 | | 0x0C | 115200 |
| 0x05 | 9600 | | 0x0D | 230400 |
| 0x06 | 14400 | | 0x0E | 460800 |
| 0x07 | 19200 | | 0x0F | 921600 |

### 6.4 发送模式说明

| 值 | 模式名称 | 说明 |
|:--:|----------|------|
| 00 | 定时发送 | 按设定间隔自动上报数据 |
| 01 | 线圈置 1 触发 | 当指定线圈被置 1 时触发上报 |
| 02 | 线圈变化触发 | 当指定线圈状态变化时触发上报 |

### 6.5 远程置数结果码

| 状态码 | 说明 |
|:------:|------|
| 0 | 成功 |
| 1 | 发送 Modbus 报文失败 |
| 2 | 解析 Modbus 报文失败 |
| 3 | 数据包长度错误 |
| 4 | 地址越界 |
| 5 | 功能码错误 |
| 6 | 数据包小于最小长度 |

---

## 七、数据库设计

### 7.1 设备表 (dev)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| Id | INT | 主键，自增 |
| Devname | VARCHAR(255) | 设备名称 |
| DevSerial | VARCHAR(255) | 设备序列号 |
| DevLocation | VARCHAR(255) | 设备位置 |
| DevStatus | VARCHAR(255) | 状态: 1=在线, 0=离线 |
| LatestOnline | DATETIME | 最后在线时间 |
| sendmodel | VARCHAR(255) | 发送模式 |
| configdata | VARCHAR(255) | 配置数据 |
| Baud | VARCHAR(255) | 波特率 |
| Changeflag | VARCHAR(255) | 修改标志 |
| successflag | VARCHAR(255) | 成功标志 |

### 7.2 用户表 (user)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| Id | INT | 主键，自增 |
| Username | VARCHAR(255) | 用户名 |
| Password | VARCHAR(255) | 密码 (MD5) |
| Nickname | VARCHAR(255) | 昵称 |
| Type | INT | 用户类型: 0=超管, 1=管理员, 2=普通, 3=只读 |

### 7.3 变量表 (variables)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| ID | INT | 主键，自增 |
| dev_ID | INT | 设备ID，外键 |
| Var_name | VARCHAR(255) | 变量名称 |
| Data_type | VARCHAR(255) | 数据类型 |
| modbus_type | VARCHAR(255) | Modbus分区 (0-4区) |
| modbus_device | INT | 站号/从设备地址 |
| modbus_addr | VARCHAR(255) | 数据地址 |
| data_len | VARCHAR(255) | 数据长度 |
| string_len | VARCHAR(255) | 字符串长度 |
| Decimal_digits | INT | 小数位数 |

### 7.4 缓存表 (caching)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| ID | INT | 主键，自增 |
| dev_ID | INT | 设备ID |
| Var_name | VARCHAR(255) | 变量名称 |
| Data_type | VARCHAR(255) | 数据类型 |
| modbus_type | VARCHAR(255) | Modbus分区 |
| modbus_device | INT | 站号 |
| modbus_addr | VARCHAR(255) | 数据地址 |
| data_len | VARCHAR(255) | 数据长度 |
| string_len | VARCHAR(255) | 字符串长度 |
| Decimal_digits | INT | 小数位数 |

### 7.5 Redis 用途

| 用途 | 说明 |
|------|------|
| Token 存储 | 用户会话管理 |
| 接口限流 | 100 次/秒 |
| 键前缀 | `Dev4G_` |

---

## 八、API 接口文档

### 8.1 用户接口

| 接口路径 | 方法 | 功能 |
|----------|:----:|------|
| `/login` | POST | 用户登录 |
| `/logout` | POST | 用户登出 |
| `/register` | POST | 用户注册 |
| `/modifyuser` | POST | 修改密码 |
| `/Permuser` | POST | 修改权限 |
| `/user` | POST | 获取用户列表 |

### 8.2 设备接口

| 接口路径 | 方法 | 功能 |
|----------|:----:|------|
| `/get-devicelist` | POST | 获取设备列表 |
| `/add/device` | POST | 添加设备 |
| `/modify-device` | POST | 修改设备 |
| `/remove/device` | POST | 删除设备 |

### 8.3 变量接口

| 接口路径 | 方法 | 功能 |
|----------|:----:|------|
| `/getvariables` | POST | 获取变量列表 |
| `/addvariable` | POST | 添加变量 |
| `/updatevariable` | POST | 更新变量 |
| `/deletevariable` | POST | 删除变量 |
| `/getvarbydeviceid` | POST | 按设备获取变量 |
| `/recoveryvariable` | POST | 恢复变量 |

### 8.4 数据接口

| 接口路径 | 方法 | 功能 |
|----------|:----:|------|
| `/payload` | POST | 下发指令 |
| `/query` | POST | 查询设备 |
| `/dataquery` | POST | 数据查询 |
| `/data` | POST | 获取数据 |
| `/alldata` | POST | 获取所有数据 |

### 8.5 请求/响应示例

**登录请求：**
```json
POST /login
{
    "username": "admin",
    "password": "123456"
}
```

**登录响应：**
```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "id": 1,
        "username": "admin",
        "type": 0,
        "token": "eyJhbGciOiJIUzI1NiIs..."
    }
}
```

---

## 九、核心模块分析

### 9.1 前端架构

**请求拦截器：**
```javascript
// src/utils/request.js
// 请求拦截器：自动添加 Token
service.interceptors.request.use(config => {
    const token = localStorage.getItem('token')
    if (token) {
        config.headers.Authorization = `Bearer ${token}`
    }
    return config
})

// 响应拦截器：处理登录失效
service.interceptors.response.use(response => {
    if (response.data.code === 66 ||
        response.data.message === 'token已失效') {
        window.location.href = '/login'
    }
    return response
})
```

### 9.2 后端架构

**请求处理流程：**
```
请求 → Controller → Service → Logic → DAO → Database
              ↓
         Middleware (CORS、认证、日志)
```

**启动流程 (`main.go`)：**
```
1. 启动定时任务 (每30秒检测设备离线状态)
2. 初始化 MQTT 客户端
3. 启动 HTTP 服务器 (端口 8000)
```

### 9.3 核心业务逻辑

| 模块 | 函数 | 功能 |
|------|------|------|
| 设备管理 | `CreateDevice` | 批量创建设备 |
| | `ModifyDevice` | 修改设备信息 |
| | `RemoveDevice` | 删除设备 |
| | `GetDevicelist` | 获取设备列表 |
| 用户管理 | `AdminLogin` | 管理员登录 (可多设备) |
| | `UserLogin` | 普通用户登录 (单设备) |
| | `Registeruser` | 用户注册 |
| | `Modifyuser` | 修改密码 |
| | `Permuser` | 修改权限 |
| MQTT | `logic/mqtt.go` | 连接 Broker: `mqtt://111.35.64.52:10213` |
| | | 订阅主题: `/dtu/{device_serial}/up` |

---

## 十、数据流转路径

### 10.1 设备上报数据流程
```
4G 设备 → MQTT Broker → 后端 MQTT 订阅 → Payload 解析 → MySQL (设备状态) + InfluxDB (时序数据)
```

### 10.2 下发指令流程
```
前端 → 后端 API → MQTT 发布 → 4G 设备
```

### 10.3 登录认证流程
```
前端登录 → 后端验证 → JWT Token 生成 → Redis 存储
                                    ↓
                             localStorage 前端存储
```

---

## 十一、配置文件说明

```yaml
# manifest/config/config.yaml

server:
  address:     ":8000"           # HTTP 服务端口

database:
  default:
    link: "mysql:root:123456@tcp(127.0.0.1:3306)/device?charset=utf8mb4"

mqtt:
  brokerURI:   "mqtt://111.35.64.52:10213"
  clientID:    "hlt"
  cleanSession: true

redis:
  host:    "172.12.0.219"
  port:    "6379"
  limiter: 100
  prefix:  "Dev4G_"

influxdb:
  token: "your-token"
  url:   "http://172.12.0.219:8086"
  org:   "myorg"
  bucket: "mybucket"
```

---

## 十二、用户操作流程

### 12.1 设备管理流程
```
① 登录系统 → ② 进入设备管理 → ③ 新建设备(名称/序列号/位置)
→ ④ 设备连接 MQTT 并上报心跳 → ⑤ 在设备列表查看在线状态
```

### 12.2 变量配置流程
```
① 点击变量管理 → ② 新建变量(名称/类型/分区/站号/地址)
→ ③ 点击下发 → ④ 设备返回结果 → ⑤ 点击撤销变更同步缓存
```

### 12.3 配置下发流程
```
① 选择设备 → ② 配置下发 → ③ 填写参数(发送模式/波特率)
→ ④ 确认下发 → ⑤ 设备执行 → ⑥ 上报结果 → ⑦ 更新状态
```

### 12.4 远程置数流程
```
① 选择设备 → ② 远程置数 → ③ 填写(数据区/地址/数量/数值)
→ ④ 确认下发 → ⑤ 设备执行 → ⑥ 返回结果
```

---

## 十三、系统部署架构

```
                            ┌─────────────────┐
                            │   用户浏览器    │
                            └────────┬────────┘
                                     │ HTTP
                            ┌────────┴────────┐
                            │   Go 后端服务   │
                            │   (端口 8000)   │
                            └────────┬────────┘
                                     │
          ┌──────────────────────────┼──────────────────────────┐
          │                          │                          │
   ┌──────┴──────┐          ┌───────┴───────┐          ┌──────┴──────┐
   │   MySQL     │          │     Redis     │          │    MQTT     │
   │ (设备/用户) │          │ (Token/限流)  │          │   Broker    │
   └─────────────┘          └───────────────┘          └──────┬──────┘
          │                                                  │
          │                                           ┌───────┴───────┐
          │                                           │   4G DTU     │
          │                                           │    设备群    │
          │                                           └───────────────┘
          │
   ┌──────┴──────┐
   │  InfluxDB   │
   │ (时序数据)  │
   └─────────────┘
```

---

## 十四、常见问题与解决方案

### 14.1 设备无法上线

| 可能原因 | 排查步骤 | 解决方案 |
|----------|----------|----------|
| 网络问题 | 检查 4G 信号 | 确认 SIM 卡正常 |
| MQTT 未连接 | 检查 Broker 地址 | 确认端口 10213 可达 |
| 序列号错误 | 核对设备序列号 | 在平台注册正确序列号 |
| 防火墙拦截 | 检查防火墙规则 | 开放相应端口 |

### 14.2 配置下发失败

| 可能原因 | 排查步骤 | 解决方案 |
|----------|----------|----------|
| 设备离线 | 检查设备状态 | 确保设备在线 |
| 波特率不匹配 | 核对波特率设置 | 两端设置一致 |
| 功能码错误 | 检查协议格式 | 确认数据格式正确 |

### 14.3 登录失败

| 错误码 | 说明 | 解决方案 |
|--------|------|----------|
| 53 | 账号或密码错误 | 检查用户名密码 |
| 54 | 账号已禁用 | 联系管理员 |
| 66 | Token 无效 | 重新登录 |

---

## 十五、总结

本项目是一个典型的**工业物联网 (IIoT) 平台**，核心价值在于：

| 价值点 | 说明 |
|--------|------|
| **远程管理** | 通过 4G 网络实现设备远程配置和控制 |
| **实时监控** | 实时获取设备状态和传感器数据 |
| **数据存储** | InfluxDB 高效存储历史时序数据 |
| **用户权限** | 完善的角色权限体系 |

**技术亮点：**

- 前后端分离架构
- Go 语言高性能处理
- Vue 3 现代化前端
- MQTT 实时通信
- InfluxDB 时序存储

**快速开发建议：**

1. 先启动后端服务，确认数据库连接正常
2. 再启动前端开发服务器
3. 使用浏览器开发者工具调试网络请求
4. 阅读 `logic/payload.go` 理解核心协议处理

---

> 📝 笔记整理完成 | 综合精炼版 v3.0
