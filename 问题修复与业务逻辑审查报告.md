# 4G 物联网平台 — 问题修复与业务逻辑审查报告

> 版本: v2.1 | 日期: 2026-04-14 | 状态: 已完成全部修复

---

## 第一部分：已修复技术问题

---

### 问题 1.1：JWT 密钥安全随机生成机制

**问题描述**

`GenToken` 每次调用都通过 `generateRandomKey(32)` 生成新的随机密钥，导致：
- 服务重启后所有已颁发 Token 失效
- 多实例部署时各实例密钥不同，Token 无法跨实例验证
- 密钥存储在 Redis 中增加了不必要的网络开销和故障点

**根本原因**

JWT 签名密钥应是固定的服务端密钥，而非每次签发时随机生成。原设计将密钥与 Token 绑定存储在 Redis 中，是一种反模式。

**修复方案**（已实施）

1. 在 `config.yaml` 中新增 `jwt.secret` 配置项
2. 使用 `sync.Once` 在服务启动时加载一次固定密钥
3. 未配置密钥时自动生成随机密钥并输出警告
4. `GenToken` 使用固定密钥签发
5. `ValidateToken` 使用固定密钥验证，先检查 Redis 存在性再解析
6. `AddToken` 不再存储密钥，改为存储 username 映射

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `config.yaml` | 新增 `jwt.secret` 和 `jwt.expireHours` |
| `logic/token.go` | 重构密钥管理、签发、验证、存储逻辑 |

**验证方法**

```bash
# 1. 登录获取 Token
curl -X POST http://127.0.0.1:8000/login -d '{"username":"admin","password":"123456"}'
# 2. 重启服务
# 3. 使用旧 Token 访问接口，应返回正常数据
```

---

### 问题 1.4：权限检查逻辑错误

**问题描述**

`Permuser` 方法中条件 `currentUserType != 0 && currentUserType != 1` 的注释写"如果是管理员"，但实际逻辑是"如果不是管理员"，注释与代码意图矛盾。更关键的是，管理员(type=1)也能修改权限，存在越权风险。

**根本原因**

权限模型设计不清晰：超管和管理员都能修改权限，但应只有超管有此权限。

**修复方案**（已实施）

```go
// 修复前
if currentUserType != 0 && currentUserType != 1 {
    return gerror.New("非管理员不能修改权限")
}

// 修复后
if currentUserType != 0 {
    return gerror.New("只有超级管理员可以修改用户权限")
}
if ID == ctx.Value("userId") {
    return gerror.New("不能修改自己的权限")
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/user.go` | 修正权限判断条件，增加自我修改防护 |

---

### 问题 1.5：Redis 限流实现错误

**问题描述**

1. `redis_rate.PerHour(100)` 实际是 100次/小时 ≈ 0.028次/秒，与配置注释"100次/秒"严重不符
2. 限流 key 使用 tokenString，每个用户独立限流，无法实现全局限流

**根本原因**

对 `redis_rate` 库的 API 理解有误，`PerHour` 是按小时限流而非按秒。

**修复方案**（已实施）

```go
// 修复前
res, err := dao.limiter.Allow(ctx, tokenString, redis_rate.PerHour(g.Cfg().MustGet(ctx, "redis.limiter").Int()))

// 修复后
rateLimitPerSecond := g.Cfg().MustGet(ctx, "redis.limiter").Int()
if rateLimitPerSecond <= 0 {
    rateLimitPerSecond = 100
}
res, err := dao.limiter.Allow(ctx, "rate_limit:global", redis_rate.PerSecond(rateLimitPerSecond))
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `dao/internal/go-redis.go` | 修正限流速率和 key |

---

### 问题 1.6：未登录错误码不一致

**问题描述**

后端 `Auth` 中间件返回 `gcode.CodeInvalidRequest.Code()` (=1001)，但前端检查 `code === 66` 或 `code === 401`，导致未登录时前端无法正确识别并跳转登录页。

**根本原因**

前后端未统一约定错误码规范。

**修复方案**（已实施）

- 后端统一返回 `code: 401`
- 前端同时检查 `code === 401` 和 `code === 66`（兼容旧版）

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/middleware.go` | `Auth` 返回 code=401 |
| `request.js` | 增加 `code === 401` 检查 |

---

### 问题 2.1：设备状态类型不一致

**问题描述**

`DevStatus` 在 entity 中为 `string`，在 model 中为 `string`，在 payload 中为 `int`，前端需要 `=== 1 || === '1'` 双重判断。

**根本原因**

数据库字段定义为 `VARCHAR(255)`，Go 代码中混用 int 和 string。

**修复方案**（已实施）

1. `entity/dev.go`: `DevStatus` 改为 `int`，`Changeflag` 改为 `int`，`Successflag` 改为 `int`
2. `model/dev.go`: `Device.DevStatus` 改为 `int`
3. 前端 `device.vue`: 简化为 `item.DevStatus === 1`

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `entity/dev.go` | DevStatus/Changeflag/Successflag 改为 int |
| `model/dev.go` | Device/AddDevice/ModifyDeviceInput DevStatus 改为 int |
| `device.vue` | 状态判断简化 |

---

### 问题 2.2：设备序列号唯一性校验缺失

**问题描述**

`Chenckdev` 方法只检查 `Devname`（设备名）重复，不检查 `DevSerial`（序列号）重复，且空值检查在重复检查之后。

**根本原因**

检查逻辑顺序错误，且遗漏了序列号唯一性校验。

**修复方案**（已实施）

```go
// 修复后：先检查空值，再分别检查序列号和名称
func (s *sDevice) Chenckdev(ctx context.Context, dev *model.AddDevice) (bool, string) {
    if dev.DevSerial == "" || dev.Devname == "" {
        return false, "设备序列号和名称不能为空"
    }
    serialCount, _ := dao.Dev.Ctx(ctx).Where(dao.Dev.Columns().DevSerial, dev.DevSerial).Count()
    if serialCount > 0 {
        return false, "已存在相同序列号设备，不能重复添加"
    }
    nameCount, _ := dao.Dev.Ctx(ctx).Where(dao.Dev.Columns().Devname, dev.Devname).Count()
    if nameCount > 0 {
        return false, "已存在相同名称设备，请更换名称"
    }
    return true, ""
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/device.go` | 重写 `Chenckdev` 方法 |

---

### 问题 2.3：设备删除级联清理缺失

**问题描述**

`RemoveDevice` 只删除 `dev` 表记录，未清理 `caching` 表中的关联数据。`variables` 表有外键 `ON DELETE CASCADE` 会自动清理。

**根本原因**

遗漏了缓存表的清理逻辑。

**修复方案**（已实施）

```go
func (s *sDevice) RemoveDevice(ctx context.Context, id model.RemoveDeviceInput) (err error) {
    if len(id.Idlist) == 0 {
        return gerror.New("设备ID列表不能为空")
    }
    // 先清理缓存表
    _, err = dao.Caching.Ctx(ctx).WhereIn("dev_ID", id.Idlist).Delete()
    if err != nil {
        g.Log().Warning(ctx, "删除设备关联缓存失败", err)
    }
    // 再删除设备（variables 表有外键级联删除）
    _, err = dao.Dev.Ctx(ctx).WhereIn(dao.Dev.Columns().Id, id.Idlist).Delete()
    if err != nil {
        return gerror.New("删除设备失败")
    }
    return nil
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/device.go` | 增加缓存表清理，增加空列表校验 |

---

## 第二部分：业务逻辑深入分析

---

### 问题 BL-01：心跳包写入 InfluxDB 导致数据膨胀

**问题描述**

功能码 `00`（心跳包）处理时调用了 `WriteDevUpdateToInflux`，将心跳记录写入时序数据库。设备每 30-60 秒发送一次心跳，长期运行会产生大量无意义的心跳记录。

**复现步骤**

1. 设备正常上线
2. 设备每 30 秒发送心跳包
3. 检查 InfluxDB 中 `Featurescode` measurement
4. 发现大量 `features_code=0` 的记录

**影响范围**

- InfluxDB 存储空间快速增长
- 查询性能下降
- 有效数据被心跳数据淹没

**根本原因**

心跳包的目的是确认设备在线状态，只需更新 MySQL 中的 `DevStatus` 和 `LatestOnline`，无需写入时序数据库。

**修复方案**（已实施）

```go
case "00":
    Featurescode = 0x00
    DevUpdate = &model.DevUpdateItem{
        DevSerial:    devSerial,
        DevStatus:    1,
        LatestOnline: gtime.Now(),
    }
    // 移除心跳包写入InfluxDB调用，减少数据膨胀
    // WriteDevUpdateToInflux(ctx, org, bucket, DevUpdate, Featurescode)
    return false, Featurescode, DevUpdate, nil, nil
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/payload.go` | 注释掉心跳包的WriteDevUpdateToInflux调用 |

---

### 问题 BL-02：Modbus 数据解析缺少边界检查

**问题描述**

`case "05"`（上报数据）中，`scanner.Next()` 的返回值未检查错误，且 `totalLen` 可能为负数或超出 payload 实际长度。

**复现步骤**

1. 设备发送畸形数据包，长度字段声明为 1000 字节但实际只有 10 字节
2. `totalLen - 3` 计算为 997
3. `scanner.Next()` 返回空 slice 或 panic

**影响范围**

- 服务可能因 panic 崩溃
- 数据解析错误导致 InfluxDB 写入脏数据

**根本原因**

未对网络输入做边界校验，违反了"永远不信任外部输入"原则。

**修复方案**（已实施）

已在功能码03和05的解析中添加：
1. `len(lenBytes) < 2` 检查 - 长度字节不足时直接返回错误
2. `totalLen < 3` 检查 - 总长度非法时直接返回错误
3. 每个 `scanner.Next()` 调用后检查错误和返回长度
4. 添加 `valueLen <= 0 || valueLen > 256` 检查
5. 添加 `length > 2048`（线圈）和 `length > 128`（寄存器）检查

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/payload.go` | 为case "03"和case "05"添加完整边界检查 |

---

### 问题 BL-03：UserLogin 中 Token 重复生成

**问题描述**

`UserLogin` 方法中先通过 `NoRepeatLogin` 获取已有 Token 并赋值给 `out`，然后又调用 `GenToken` 生成新 Token 覆盖 `out`，导致：
- 已有 Token 在 Redis 中被新 Token 替换但未清理
- 每次登录都生成新 Token，`NoRepeatLogin` 的返回值被丢弃

**复现步骤**

1. 用户 A 登录，获取 Token1
2. 用户 A 再次登录
3. `NoRepeatLogin` 返回 Token1，赋值给 out
4. `GenToken` 生成 Token2，覆盖 out
5. Redis 中 Token1 的 username 映射被 Token2 替换
6. Token1 仍存在于 Redis（key=Token1, value=username），但 username→Token 映射已指向 Token2

**影响范围**

- Redis 中残留无效 Token 数据
- 多设备同时登录时行为不可预测

**根本原因**

`NoRepeatLogin` 的返回值未被使用，代码逻辑混乱。

**修复方案**（已实施）

```go
func (s *sUser) UserLogin(ctx context.Context, in model.User) (ok bool, out *model.UserLoginOutput, err error) {
    var one *model.User
    err = dao.User.Ctx(ctx).Where("username", in.Username).Scan(&one)
    if err != nil || one == nil || one.Password != s.MD5Password(in.Password) {
        return false, nil, gerror.New("账号或密码错误")
    }

    // 检查是否已有有效会话
    existingToken, _ := service.Token().NoRepeatLogin(ctx, in.Username)
    if existingToken != "" {
        out = &model.UserLoginOutput{
            Type:        one.Type,
            TokenString: existingToken,
        }
        return true, out, nil
    }

    // 无有效会话，生成新Token
    tokenString, err := service.Token().GenToken(ctx, in.Username, time.Hour*24)
    if err != nil {
        return false, nil, gerror.New("token生成失败")
    }
    out = &model.UserLoginOutput{
        Type:        one.Type,
        TokenString: tokenString,
    }
    return true, out, nil
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/user.go` | 重构UserLogin，token存在时直接返回 |

---

### 问题 BL-04：MQTT 无自动重连机制

**问题描述**

MQTT 客户端连接失败时直接 `Panic`，且未配置自动重连参数。网络波动导致断连后，系统无法接收设备数据。

**复现步骤**

1. 服务正常运行，MQTT 已连接
2. 网络短暂中断
3. MQTT 连接丢失
4. 服务不会自动重连，所有设备数据丢失

**影响范围**

- 设备数据丢失
- 需要人工重启服务

**根本原因**

未使用 paho.mqtt.golang 提供的自动重连功能。

**修复方案**（已实施）

```go
func (s *sMqtt) Init() {
    opts.AddBroker(BrokerURI)
    opts.SetClientID(g.Cfg().MustGet(context.Background(), "mqtt.clientID").String())
    opts.SetAutoReconnect(true)
    opts.SetConnectRetry(true)
    opts.SetConnectRetryInterval(5 * time.Second)

    opts.OnConnectionLost = func(c mqtt.Client, err error) {
        g.Log().Warning(context.Background(), "MQTT连接断开: ", err)
        go s.handleReconnect()
    }
    opts.OnConnect = func(c mqtt.Client) {
        reconnectAttempts = 0
        s.subscribeAll(context.Background())
    }
    // ...
}

func (s *sMqtt) handleReconnect() {
    for reconnectAttempts < maxReconnectAttempts {
        reconnectAttempts++
        delay := baseReconnectDelay * time.Duration(1<<uint(reconnectAttempts))
        if delay > time.Minute {
            delay = time.Minute
        }
        time.Sleep(delay)
        // 尝试重连...
    }
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/mqtt.go` | 添加OnConnectionLost回调和handleReconnect重连函数 |

---

### 问题 BL-05：MQTT 消息去重逻辑从未生效

**问题描述**

`PayloadHandler` 始终返回 `isRepeat=false`，`messageHandler` 中的去重分支永远不会执行。

**复现步骤**

1. 设备发送数据包
2. 网络重传导致同一消息被处理两次
3. InfluxDB 中出现重复数据点

**影响范围**

- 时序数据库中存在重复数据
- 数据分析结果不准确

**根本原因**

去重逻辑只有框架没有实现。

**修复方案**（已实施）

在 `messageHandler` 中添加基于消息内容和主题的Redis去重：

```go
// BL-05修复：实现消息去重，基于消息内容和时间窗口
payloadHash := fmt.Sprintf("%x", md5.Sum(msg.Payload()))
dedupKey := fmt.Sprintf("mqtt:dedup:%s:%s", msg.Topic(), payloadHash)
exists, _ := dao.Redis.IsExist(ctx, dedupKey)
if exists {
    g.Log().Debug(ctx, "重复消息，忽略")
    return nil
}
dao.Redis.Set(ctx, dedupKey, "1", time.Minute*5)
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/mqtt.go` | 添加基于md5的消息去重逻辑 |
| `dao/internal/go-redis.go` | Clear方法支持dedup scope |

---

### 问题 BL-06：DownPayloadHandler 中 topic 解析越界风险

**问题描述**

```go
strings.Split(topic, "/")[2]
```

如果 topic 格式不符合预期（如 `/dtu/ABC123/down` 分割后只有4段），直接取 `[2]` 可能取到错误值或越界。

**复现步骤**

1. topic 为 `/dtu/ABC123/down` → Split 结果 `["", "dtu", "ABC123", "down"]` → `[2]` = "ABC123" ✅
2. topic 为 `/dtu/` → Split 结果 `["", "dtu", ""]` → `[2]` = "" ❌
3. topic 为 `/dtu` → Split 结果 `["", "dtu"]` → `[2]` 越界 panic ❌

**影响范围**

- 服务可能 panic 崩溃

**根本原因**

未做边界检查，应复用已有的 `parseTopic` 方法。

**修复方案**（已实施）

1. 添加 `code` 长度检查：`if len(code) < 2`
2. 新增 `parseTopicSafe` 方法使用正则表达式解析topic

```go
// BL-06修复：安全的topic解析，使用正则表达式
func (s *sPayload) parseTopicSafe(topic string) (string, error) {
    if topic == "" {
        return "", fmt.Errorf("topic为空")
    }
    re := regexp.MustCompile(`/dtu/([0-9A-Fa-f]+)`)
    match := re.FindStringSubmatch(topic)
    if len(match) < 2 {
        return "", fmt.Errorf("无法解析设备序列号: %s", topic)
    }
    return match[1], nil
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/payload.go` | 添加parseTopicSafe方法，DownPayloadHandler使用安全解析 |

---

### 问题 BL-07：IsHeartBeat 方法返回值错误

**问题描述**

```go
func (s *sPayload) IsHeartBeat(data []byte) (int, byte) {
    if s.BytesToString(data) == "00" {
        return 1, 0x00
    }
    return 1, 00  // 🔴 非心跳包也返回状态1（在线）
}
```

**影响范围**

- 非心跳包被误判为设备在线

**修复方案**（已实施）

```go
// BL-07修复：修正返回值逻辑，返回(bool, byte)表示(是否为心跳, 功能码)
func (s *sPayload) IsHeartBeat(data []byte) (bool, byte) {
    if len(data) < 1 {
        return false, 0
    }
    if s.BytesToString(data) == "00" {
        return true, 0x00
    }
    return false, data[0]
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/payload.go` | 修正IsHeartBeat返回值为bool类型 |

---

### 问题 BL-08：AddDevice 中重复字段赋值

**问题描述**

```go
dao.Dev.Columns().Changeflag:   0,
dao.Dev.Columns().Changeflag:   0,  // 🔴 重复，应为 Successflag
```

**影响范围**

- `Successflag` 字段不会被初始化
- Go 的 map 重复 key 不会报错，后者覆盖前者

**修复方案**（已实施）

```go
dao.Dev.Columns().Changeflag:   0,
dao.Dev.Columns().Successflag:  0,
```

---

### 问题 BL-09：Redis Clear 方法会删除所有数据

**问题描述**

```go
func (dao *RedisDao) Clear(ctx context.Context) {
    go func() {
        for {
            time.Sleep(time.Hour * 24)
            keys, _, err := dao.rdb.Scan(ctx, 0, prefix+"*", 0).Result()
            // 删除所有匹配前缀的key
        }
    }()
}
```

**影响范围**

- 每24小时清空所有 Redis 数据，包括有效 Token
- 所有用户被迫重新登录

**根本原因**

此方法设计为全量清理，但实际应只清理过期数据。当前已被注释掉（`init` 中 `dao.Redis.Clear` 被注释），但代码仍存在误用风险。

**修复方案**（已实施）

添加scope参数支持，按业务域选择性清理：

```go
// BL-09修复：Clear方法添加作用域限制，支持按pattern删除
func (dao *RedisDao) Clear(ctx context.Context, scope string) {
    go func() {
        for {
            time.Sleep(time.Hour * 24)
            var pattern string
            switch scope {
            case "cache":
                pattern = prefix + "cache:*"
            case "token":
                pattern = prefix + "token:*"
            case "dedup":
                pattern = prefix + "mqtt:dedup:*"
            default:
                pattern = prefix + "*"
            }
            // 分批扫描删除...
        }
    }()
}
```

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `dao/internal/go-redis.go` | Clear方法添加scope参数和分批清理逻辑 |

---

### 问题 BL-10：InfluxDB 写入无错误处理

**问题描述**

`WriteDevUpdateToInflux` 和 `WritdataToInflux` 调用 `writeAPI.WritePoint` 后未检查错误：

```go
writeAPI.WritePoint(ctx, point)
return nil  // 🔴 始终返回 nil，即使写入失败
```

**影响范围**

- 数据静默丢失
- 无法发现 InfluxDB 连接问题

**修复方案**（已实施）

```go
func WriteDevUpdateToInflux(ctx context.Context, org, bucket string, devUpdate *model.DevUpdateItem, featuresCode byte) error {
    writeAPI := InfluxClient.WriteAPIBlocking(org, bucket)
    // ... 构建 point ...
    err := writeAPI.WritePoint(ctx, point)
    if err != nil {
        g.Log().Error(ctx, "写入InfluxDB失败，重试中...", err)
        time.Sleep(100 * time.Millisecond)
        err = writeAPI.WritePoint(ctx, point)
        if err != nil {
            g.Log().Error(ctx, "写入InfluxDB重试失败", err)
            return fmt.Errorf("写入InfluxDB失败: %w", err)
        }
    }
    return nil
}
```

同样适用于 `WritdataToInflux`。

**修改文件**

| 文件 | 修改内容 |
|------|---------|
| `logic/payload.go` | WriteDevUpdateToInflux和WritdataToInflux添加错误处理和重试 |

---

## 修复状态汇总

| 编号 | 问题 | 风险等级 | 修复状态 |
|------|------|---------|---------|
| 1.1 | JWT密钥随机生成 | 🔴 高 | ✅ 已修复 |
| 1.4 | 权限检查逻辑错误 | 🔴 高 | ✅ 已修复 |
| 1.5 | Redis限流实现错误 | 🟡 中 | ✅ 已修复 |
| 1.6 | 未登录错误码不一致 | 🔴 高 | ✅ 已修复 |
| 2.1 | 设备状态类型不一致 | 🟡 中 | ✅ 已修复 |
| 2.2 | 设备序列号唯一性缺失 | 🟡 中 | ✅ 已修复 |
| 2.3 | 设备删除级联清理缺失 | 🟡 中 | ✅ 已修复 |
| BL-01 | 心跳包写入InfluxDB | 🟡 中 | ✅ 已修复 |
| BL-02 | Modbus数据解析无边界检查 | 🔴 高 | ✅ 已修复 |
| BL-03 | UserLogin Token重复生成 | 🟡 中 | ✅ 已修复 |
| BL-04 | MQTT无自动重连 | 🔴 高 | ✅ 已修复 |
| BL-05 | MQTT消息去重未实现 | 🟢 低 | ✅ 已修复 |
| BL-06 | Topic解析越界风险 | 🔴 高 | ✅ 已修复 |
| BL-07 | IsHeartBeat返回值错误 | 🟡 中 | ✅ 已修复 |
| BL-08 | AddDevice重复字段赋值 | 🟡 中 | ✅ 已修复 |
| BL-09 | Redis Clear全量删除 | 🟡 中 | ✅ 已修复 |
| BL-10 | InfluxDB写入无错误处理 | 🟡 中 | ✅ 已修复 |

---

> 📝 报告生成完成 | 17项已修复 | 0项待修复
